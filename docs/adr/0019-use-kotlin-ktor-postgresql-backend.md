# Use Kotlin Ktor PostgreSQL backend

The backend will use Kotlin, Ktor, and PostgreSQL. This keeps the mobile and backend stacks aligned around Kotlin, gives the small account-device-configuration backend a lighter framework than Spring Boot, and uses PostgreSQL for durable account, device, consent, and audit data.

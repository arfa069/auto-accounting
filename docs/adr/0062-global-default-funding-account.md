# ADR 0062: 全局默认资金账户

状态：接受

资金账户默认值是跨账本、按登录账号同步的可选设置，仅覆盖新建或待确认最终入账；编辑既有账目继续使用显式选择。默认值只保存 `fundingAccount.syncId`，不进入账本同步或加密备份。

本地写入先完成，云端同步失败保留待办并允许重试；旧后端能力标记为不支持时不报告同步成功。账号切换按账号键隔离缓存，删除默认账户时在同一事务清除指针。
